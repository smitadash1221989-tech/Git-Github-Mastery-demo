Soft Reset - the last commit is resetted but the files are in staged and the file is in working directory

Create

Commit 1
Commit 2
Commit 3

Practice

Undo last commit but keep staged changes.

git reset --soft HEAD~1
the history is rewritten,it removes the commit3 the last commit from the commit history and keeps the changes staged

Mixed Reset - the files are in working diectory but now in unstaged state - 
Undo commit. mixed reset is same as normal reset
Keep files.
Remove from staging.

Hard Reset- the files are removed completelty from the working directory
in all the case of reset the history is rewritten with the different states of working directotry and staging area conditions
Undo last commit completely.
Recover it using reflog.
git reflog
git reset --hard commitid - always take the commit id just before the delete commit so that all the details prior to delete will be recovered

Reset Specific File
git add .
git restore --staged filename
the files are unstaged

echo "this is a dummy file">file1.txt
git status
git restore file1.txt
git status 
the changes done in file1 is resetted

Stage multiple files.
 git add .
git status - showss the staged status of each file
git restore --staged
Unstage one.

Reset to Older Commit

Move branch backward.

Recover latest commit.